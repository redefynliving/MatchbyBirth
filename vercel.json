{
  "buildCommand": "cd apps/web && npm install && npm run build",
  "outputDirectory": "dist/apps/web",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/apps/web/api/$1.js" }
  ]
}
